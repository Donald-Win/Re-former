[build]
  command = "cp manifest.netlify.json manifest.json && cp index.netlify.html index.html && npm run build -- --config vite.config.netlify.js"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

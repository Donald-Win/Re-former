[build]
  # index.html and manifest.json are now shared with the GitHub Pages build
  # (see index.html's %BASE_URL% and manifest.json's relative paths) —
  # no more `cp` swap step needed here.
  command = "BASE_PATH=/ npm run build"
  publish = "dist"
  ignore = "[ \"$BRANCH\" = \"dev\" ]"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
